# Dev containers run pnpm with no TTY: when the bind-mounted lockfile drifts
# from the image's node_modules, pnpm's purge prompt would abort the start.
# Answer it non-interactively so the dev server reinstalls and boots.
confirm-modules-purge=false
