// The crypto lives in the parity package so the frozen cross-stack vectors
// (shared/parity-json/e2ee.json) replay against the exact code this app ships.
export 'package:nostragoalus_parity/e2ee.dart';
