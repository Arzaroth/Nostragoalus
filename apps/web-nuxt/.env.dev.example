# Dev-only overrides, loaded on top of .env by the compose.dev.yaml overlay.
# Copy to .env.dev (gitignored) and adjust as needed.
# NUXT_SMTP_URL is already set to maildev by the overlay; override here if needed.
#NUXT_SMTP_URL=smtp://maildev:1025
#NUXT_SMTP_FROM=Nostragoalus Dev <dev@nostragoalus.local>
