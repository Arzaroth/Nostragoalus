# Dependencies
node_modules
test-results
playwright-report
.playwright

# Nuxt / Nitro build + dev artifacts
.nuxt
.output
.nitro
.data
.cache
dist

# Logs
logs
*.log
pnpm-debug.log*

# Environment / secrets
.env
.env.*
!.env.example
!.env.dev.example
# the e2e stack URLs/ports (no secrets) are version-controlled so e2e targets the
# isolated stack by default
!.env.e2e

# OS / editor
.DS_Store
.idea
.fleet
.vscode/*
!.vscode/extensions.json

# Test / coverage
coverage

# Misc
*.local

# Note: Drizzle migrations (./drizzle/**) are committed on purpose.

# curl cookie jars from E2E/manual testing (never commit session tokens)
*.jar
cookies.txt

# Claude Code local state (skills and commands ARE tracked)
.claude/worktrees/
.claude/settings.local.json

# Database backups (mise run db-backup)
backups/

# pnpm content-addressable store (created by store-dir in repo)
.pnpm-store

# Local code-review artifacts (feature-treatment / review sessions)
reviews/
